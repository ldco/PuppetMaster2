# /pm apply — Apply Contribution to PM Framework

**Purpose**: Apply a contribution document generated by `/pm contribute` from a client project. This implements the fix/feature into the Puppet Master framework.

This command is used in the **PM framework repository** (not client projects).

## Usage

```
/pm apply                         # Look for .pm-contribution.md in root
/pm apply ./path/to/contribution.md   # Specify file path
/pm apply --review                # Review only, don't apply
```

---

## EXECUTE These Steps

### Step 1: Find Contribution File

Look for contribution document:

```bash
ls .pm-contribution.md *.pm-contribution.md 2>/dev/null
```

Or use provided path.

**If not found:**
```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
                      ⚠️  NO CONTRIBUTION FILE FOUND
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Copy the contribution file from your client project:

  cp ~/client-project/.pm-contribution.md ./

Then run /pm apply again.
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

### Step 2: Read and Parse Contribution

Read the contribution file using Read tool.

Parse the markdown to extract:
- **Meta**: ID, Type, Priority, Created
- **Summary**: One-line description
- **Problem**: What was the issue
- **Solution**: How it was solved
- **Domains Affected**: Checklist of areas
- **Files Changed**: Modified, Created, Deleted with diffs/content
- **Dependencies**: New packages needed
- **Breaking Changes**: What might break
- **Testing**: How to verify

### Step 3: Display Contribution Summary

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
                         📥 PM CONTRIBUTION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ID:        {id}
Type:      {type}
Priority:  {priority}
Created:   {date}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

SUMMARY
─────────────────────────────────────────────────────────────────────────────
{summary}

PROBLEM
─────────────────────────────────────────────────────────────────────────────
{problem}

SOLUTION
─────────────────────────────────────────────────────────────────────────────
{solution}

DOMAINS AFFECTED
─────────────────────────────────────────────────────────────────────────────
{list of checked domains}

FILES TO CHANGE
─────────────────────────────────────────────────────────────────────────────
  Modified: {count} files
  Created:  {count} files
  Deleted:  {count} files

{list files}

DEPENDENCIES
─────────────────────────────────────────────────────────────────────────────
{none | list}

BREAKING CHANGES
─────────────────────────────────────────────────────────────────────────────
{none | description}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

**If --review flag, stop here.**

### Step 4: Check for Conflicts

For each file to be modified, check if it exists and compare:

```bash
ls {file_path} 2>/dev/null
```

Read current file content and compare with the "old" part of the diff.

**If conflict detected:**
```
⚠️  Potential conflict in {file}:
    The current file differs from the expected base.
    This may indicate the file was modified since the contribution was created.

    Options:
    1. View current file and contribution diff
    2. Apply anyway (manual merge may be needed)
    3. Skip this file
    4. Abort
```

Use AskUserQuestion to resolve.

### Step 5: Confirm Application

```
Ready to apply this contribution?

This will:
  • Modify {X} files
  • Create {Y} files
  • Delete {Z} files
  {• Install dependencies: ...}

Proceed? [Y/n]
```

### Step 6: Install Dependencies (if any)

If new dependencies are listed:

```bash
pnpm add {package1} {package2}
```

Or for dev dependencies:
```bash
pnpm add -D {package}
```

### Step 7: Apply File Changes

**For each modified file:**

1. Read the diff from contribution
2. Parse the old/new content
3. Use Edit tool to apply changes
4. Verify the edit was successful

**For each created file:**

1. Read the full content from contribution
2. Use Write tool to create file
3. Verify the file was created

**For each deleted file:**

1. Confirm deletion
2. Use Bash to remove: `rm {file_path}`

### Step 8: Verify Changes

After applying all changes:

```bash
# Check for TypeScript errors
pnpm run lint 2>&1 | head -20

# Check if build works
pnpm run build 2>&1 | head -20
```

Report any errors found.

### Step 9: Display Testing Instructions

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
                      📋 TESTING INSTRUCTIONS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

{testing instructions from contribution}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

### Step 10: Summary and Next Steps

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
                      ✅ CONTRIBUTION APPLIED
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Applied: {id} - {summary}

Changes:
  ✅ Modified {X} files
  ✅ Created {Y} files
  ✅ Deleted {Z} files
  {✅ Installed dependencies}

Verification:
  {✅ Lint passed | ⚠️ Lint warnings | ❌ Lint errors}
  {✅ Build passed | ❌ Build failed}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Next Steps:
  1. Run tests: pnpm test
  2. Test manually using instructions above
  3. If all good, commit:
     /commit "feat: {summary}"
  4. Remove contribution file:
     rm .pm-contribution.md

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

## Conflict Resolution

When the current file doesn't match expected base:

### Option 1: View Comparison

Show:
- Current file content (relevant section)
- Expected "old" content from diff
- Proposed "new" content from diff

### Option 2: Apply Anyway

Apply the new content, warn user to review manually.

### Option 3: Skip File

Skip this file, continue with others. Add to "skipped" list.

### Option 4: Abort

Stop the entire apply process. No changes made.

---

## Rollback

If something goes wrong mid-apply:

```
⚠️  Error occurred during application.

Files successfully changed:
  ✅ {list of files changed so far}

Files not yet changed:
  ⏸️ {remaining files}

Options:
  1. Continue from where we left off
  2. Rollback changes (git checkout)
  3. Abort and leave as-is
```

For rollback:
```bash
git checkout -- {list of changed files}
```

---

## Notes

- Always verify file exists before modifying
- Use Edit tool for precise changes, not full file rewrites
- Check for conflicts before applying
- Run lint/build to catch errors early
- Keep contribution file until confirmed working
- Suggest meaningful commit message based on contribution
